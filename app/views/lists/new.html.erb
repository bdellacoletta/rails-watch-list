<h1>New List</h1>

<%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit %>
<% end %>

<br>

<%= link_to 'Home', lists_path, class: "btn btn-secondary" %>
